# Production Environment Configuration
NODE_ENV=production

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Start@0212
DB_NAME=universal_hmis
DB_PORT=3306

# Server Configuration
SERVER_PORT=3001
HOST=0.0.0.0

# Security
SESSION_SECRET=your-secure-session-secret-change-this-in-production

# CORS - Allow all origins (already configured in code)
CORS_ORIGIN=*
